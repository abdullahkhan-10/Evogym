export enum allPages {
  Home = "home",
  Benefits = "benefits",
  OurClasse = "ourclasses",
  ContactUs = "contactus",
}

export interface BenefitType{
  icon: JSX.Element,
  title: string,
  description: string,
}